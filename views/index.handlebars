<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Burgers!</title>
	<script src="https://code.jquery.com/jquery-3.1.1.js"></script>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/css/bootstrap.min.css" integrity="sha384-/Y6pD6FV/Vv2HJnA6t+vslU6fwYXjCFtcEpHbNJ0lyAFsXTsjBbfaDjzALeQsN6M" crossorigin="anonymous">
  <link rel="stylesheet" href="/assets/css/burger_style.css" type="text/css" />
</head>
<body>
<div class="container">

	<div class="text-center">
    <img class="text-center" src="/assets/img/burger.png">
    <h1 class="display-3 text-center">Burgers!</h1> 
    <p class="lead">Add a burger, then click on it to eat it. It's pretty straightforward.</p>
  </div>

  <div class="row" id="burgersRow">
    <div class="col-md-8" id="added-burgers">
        <div class="row">
          <div class="col-md-12">
            <h3>On The Menu</h3>
          </div>
        </div>
        {{#each burgers}}
          {{#unless this.devoured}}
            <div class="row">
              <form action="/{{this.id}}?_method=PUT" method="POST">
                <input type="hidden" name="devoured" value="true">
                <button class="btn btn-info col-md-12" type="submit">{{this.burger_name}}</button>
              </form>
            </div>
          {{/unless}}
        {{/each}}
      
    </div>
    <div class="col-md-4" id="eaten-burgers">
      <h3>Burgers Devoured</h3>
        {{#each burgers}}
          {{#if this.devoured}}
            <div class="panel panel-default col-md-12">
                <p>{{this.burger_name}}</p>
            </div>
          {{/if}}
        {{/each}}
      
    </div>
  </div>
  <div class="row justify-content-md-center">
    <div class="col-md-4">

      <form class="create-update-form" action="/" method="POST">

        <div class="form-group">
          <input type="text" class="form-control" id="add-burger-text" placeholder="BURGER GOES HERE" name="burger_name">
        </div>

        <div class="row justify-content-md-center">
          <button type="submit" id="add-burger-submit" class="btn btn-info">Add Burger</button>
        </div>

      </form>
      
    </div>
  </div>

</div>

</body>

</html>